on: [push]

jobs:
  s3_job:
    runs-on: ubuntu-latest
    name: S3 test job
    steps:
    - name: copy repo
      uses: actions/checkout@master
    - name: Run s3 copy
      uses: rjmendus/github-action-s3-upload@master
